# CLAUDE.md

Claude Code agents should follow the canonical repo instructions in
`AGENTS.md`.

This file is intentionally short to avoid maintaining duplicate agent rules.
Read `AGENTS.md`, then read active `memory-bank/*.md` before non-trivial work.

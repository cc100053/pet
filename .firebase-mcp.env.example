# Local, untracked ADC configuration for Firebase MCP.
# Copy this file to .firebase-mcp.env and point at a service-account JSON key
# that stays outside the repo.

export GOOGLE_APPLICATION_CREDENTIALS="$HOME/.config/pet/firebase-crashlytics-sa.json"
